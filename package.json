{
    "name": "app",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "npm start --prefix server",
        "heroku-postbuild": "npm start --prefix client"
    },
    "dependencies": {
        "mongodb": "^3.2.7",
        "react-scripts": "3.0.1"
    },
    "engines": {
        "node": "10.15.0"
    }
}
